const order=[];const lines=document.querySelector('.order-lines');const empty=document.querySelector('.empty-order');const total=document.querySelector('.total b');const summary=document.querySelector('.order-summary');const checkout=document.querySelector('.checkout');const note=document.querySelector('.checkout-note');function renderOrder(){if(!lines)return;lines.innerHTML=order.map(item=>`<li><span>${item.name}</span><b>$${item.price.toFixed(2)}</b></li>`).join('');const amount=order.reduce((sum,item)=>sum+item.price,0);total.textContent=`$${amount.toFixed(2)}`;empty.hidden=order.length>0;summary.classList.toggle('is-empty',order.length===0)}document.querySelectorAll('[data-item]').forEach(button=>button.addEventListener('click',()=>{order.push({name:button.dataset.item,price:Number(button.dataset.price)});button.textContent='Added!';renderOrder();setTimeout(()=>button.textContent='Add +',700)}));if(checkout)checkout.addEventListener('click',()=>{if(!order.length)return;note.textContent='Thanks! Your pickup request is ready to be connected to a real ordering system.'});renderOrder();
